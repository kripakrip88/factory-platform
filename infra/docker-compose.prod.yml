services:
  postgres:
    restart: always

  redis:
    restart: always

  n8n:
    restart: always
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true

  telegram-bot:
    restart: always

  nginx:
    restart: always

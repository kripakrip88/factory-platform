# -*- coding: utf-8 -*-
{
    "name": "ПМК Парк — сделки",
    "summary": "Карточка сделки и канбан воронки в виде остальной системы",
    "description": """
Экран CRM был единственным из восьми в прототипе владельца, где кастомизации не
было вообще: воронка и карточка сделки оставались в базовом виде Odoo, а все
соседние экраны (контрагент, КП, производство, калькуляторы) уже собраны
приёмами темы. Из-за этого CRM выглядел чужим разделом в своей же системе.

Прототип честно ставил вопрос: заводить ли отдельный модуль или хватит шапки
поверх штатной формы. Ответ — отдельный модуль, и причина не в объёме кода
(его тут на два файла), а в том, куда складывать следующее. Правки CRM иначе
расползлись бы по pmk_theme (тема не должна знать про crm.lead) или по
pmk_partner (контрагент — не сделка). Отдельный модуль ещё и выключается
целиком одной строкой, если владелец решит, что штатный вид ему ближе.

Что внутри:

* карточка сделки — шапка документа `.pmk-doc-head` (клиент, ответственный,
  стадия) и показатели `.pmk-kpi` (сумма, вероятность) вместо штатного
  крупного заголовка с суммой и процентом;
* канбан воронки — полоса вероятности `.pmk-meter*` и отметка просроченной
  активности `.pmk-badge` на карточке сделки.

Чего внутри НАМЕРЕННО нет:

* маржи. Модуль `sale_margin` не установлен, а себестоимость (`standard_price`)
  пуста у всех 752 карточек номенклатуры: показатель посчитался бы ровно в 100%
  на каждой строке. Выглядел бы рабочим и был бы неверным;
* плашек-показателей над воронкой (Сделок в работе / Сумма воронки / Новых за
  неделю / Просрочено активностей). Объявить их в XML в Odoo 19 нечем:
  атрибут `banner_route`, которым в прежних версиях над видом вешали свою
  html-полосу, из ядра убран — проверено грепом по всему дереву 19.0, ноль
  вхождений. Остаётся своя OWL-надстройка над рендерером канбана, а её нельзя
  выкатывать без прогона в браузере: ошибка в рендерере гасит не плашку, а весь
  экран воронки. Разбор с готовым планом — в комментарии к
  `views/crm_lead_kanban_views.xml`.
""",
    "version": "19.0.1.0.0",
    "category": "Sales/CRM",
    "author": "ПМК Парк",
    "license": "LGPL-3",
    # Зависимости ровно две, и это не экономия, а требование: во время загрузки
    # Odoo собирает представление ТОЛЬКО из уже загруженных модулей. Значит
    # каждый узел, на который мы ссылаемся xpath'ом, обязан принадлежать
    # модулю отсюда. Все наши якоря — узлы самого `crm`.
    #
    # Прецедент, стоивший сломанной карточки контрагента (2026-09-20): там
    # xpath смотрел на страницу, которую создаёт модуль, ЗАВИСЯЩИЙ от нашего.
    # Его на момент сборки вида ещё не существовало, и вид пришлось выключить.
    #
    # pmk_theme — ради классов оформления (.pmk-doc-head, .pmk-kpi, .pmk-badge,
    # .pmk-meter). Без него разметка останется, а вида не будет.
    "depends": ["crm", "pmk_theme"],
    "data": [
        "views/crm_lead_views.xml",
        "views/crm_lead_kanban_views.xml",
    ],
    "installable": True,
    "application": False,
    "auto_install": False,
}

# -*- coding: utf-8 -*-
{
    # Участок лазерной резки. Порядок работы здесь ПЕРЕВЁРНУТ относительно
    # обычного планирования: чёткой статистики резки на заводе не ведётся,
    # поэтому сначала копится слой реального замера, и только из него
    # выводится норматив. Планировать по каталожной скорости станка нельзя —
    # она не знает ни нашего металла, ни нашей раскладки.
    #
    # Разбор чертежа нужен с первого дня не ради красоты, а потому что он даёт
    # ЗНАМЕНАТЕЛЬ каждому замеру. «Лист резался 47 минут» непереносимо на
    # другой заказ, пока не известно, сколько в нём было метров реза, проколов
    # и какая толщина.
    "name": "ПМК: лазерная резка",
    "version": "19.0.0.1.0",
    "summary": "Файлы раскроя CypCut, замер резки, норматив по замерам, премия операторам",
    "description": """
Что модуль делает.

1. Читает управляющий файл CypCut (.lxds) и раскладывает его по документу:
   состав заказа, физические листы, габарит и использование каждого листа,
   число контуров. Раскладка, повторённая семь раз, разворачивается в семь
   листов — иначе полезный вес занижается ровно в семь раз.
2. Читает чертёж детали (.dxf) и даёт знаменатель: метры реза и проколы.
3. Принимает замер оператора в два касания — «начал» и «закончил».
4. Выводит норматив ОБРАТНЫМ СЧЁТОМ из накопленных замеров, а не берёт его
   из паспорта станка.
5. Считает полезный вес, премию операторов и баланс металла.

Две вещи, на которых держатся деньги.

ПОЛЕЗНЫЙ ВЕС — вес разложенных деталей, а не купленного листа. За детали
платит заказчик, от них же считается премия. От веса листа считать нельзя:
тогда за плохую раскладку платят больше, чем за хорошую.

МАССА КВАДРАТА БЕРЁТСЯ ИЗ СПРАВОЧНИКА pmk_calc, а не через плотность. У
рифлёного листа квадратный метр на 5-7% тяжелее гладкого той же толщины, у
просечно-вытяжного остаётся 37-63% габарита. Формула «площадь × толщина ×
7850» на них соврёт, справочник — знает.

Контрольные якоря (три живых файла за 08.09.2026, считано владельцем вручную):
  Отводы 10 мм, 4 листа 1500x6000, 66,7/65,0/51,7/25,6%  -> 1476 кг -> 738 ₽
  Кронштейны 2 мм, 8 листов 1000x4000, 89,2% x7 и 40,2%  ->  417 кг -> 1252 ₽
  Площадка ГРПШ 3 мм, 1 лист 1500x3000, 73,0%            ->   77 кг ->   39 ₽
Если после правки кода эти три строки разошлись — сломан расчёт, а не якорь.

Чему в файле раскроя верить НЕЛЬЗЯ. Раздел Technical/content.xml у технолога
демонстрационный: во всех трёх присланных файлах он побайтово одинаков (один
MD5), с Thickness="1.5" и «чёрной сталью» — на 2, 3 и 10 мм. Поэтому толщина
берётся из ИМЕНИ файла, куда её пишут для оператора станка, а ширина реза —
своя константа в задании.
""",
    "category": "Manufacturing",
    "author": "ПМК Парк",
    "license": "LGPL-3",
    # Каждая зависимость проверена по базе стенда (ir_module_module), а не
    # взята на память — на 20.09.2026 все пять уже installed:
    #   mail  — переписка задания, message_post о результате разбора файла;
    #   hr    — оператор смены (hr.employee), от него же берётся состав для
    #           делёжки премии и автор замера (env.user.employee_id);
    #   sale  — необязательная ссылка на заказ: полезный вес и время резки
    #           надо уметь отнести на заказ, а не на участок вообще;
    #   mrp   — необязательная ссылка станка на рабочий центр. Наследования
    #           mrp.workcenter НЕТ намеренно, см. models/machine.py;
    #   pmk_calc — справочник листа. Зависимость НАСТОЯЩАЯ: масса квадрата
    #           берётся только оттуда (pmk.metal.sheet.mass_per_sqm), своей
    #           копии справочника здесь нет и быть не должно.
    "depends": [
        "mail",
        "hr",
        "sale",
        "mrp",
        "pmk_calc",
    ],
    # ezdxf объявлен зависимостью, а не «проверяется при нажатии кнопки».
    # Причина: без него Odoo откажет в установке фразой про внешнюю
    # зависимость, а с проверкой в рантайме технолог получил бы трейсбек
    # ModuleNotFoundError на кнопке «Разобрать чертежи» — и уже после того,
    # как завёл задание. Пакет добавлен в ARG PYDEPS образа
    # (experiments/odoo/Dockerfile); до пересборки образа модуль НЕ
    # установится, и это правильное поведение, а не помеха.
    "external_dependencies": {"python": ["ezdxf"]},
    # Порядок не случайный:
    #   права      — до всего, иначе загрузка данных пойдёт без доступа;
    #   станки     — на них ссылается форма задания;
    #   задание    — в нём определён view_laser_job_search;
    #   замеры/нормативы/обрезки — ссылаются на задание;
    #   загрузка   — переиспользует view_laser_job_search из задания,
    #                поэтому идёт ПОСЛЕ него, иначе ref упадёт при установке;
    #   меню       — последним: каждый пункт ссылается на готовое действие.
    "data": [
        "security/ir.model.access.csv",
        "data/ir_sequence.xml",
        "views/laser_machine_views.xml",
        "views/laser_job_views.xml",
        "views/laser_measure_views.xml",
        "views/laser_norm_views.xml",
        "views/laser_offcut_views.xml",
        "views/laser_load_views.xml",
        "views/menus.xml",
    ],
    "installable": True,
    "application": True,
    "auto_install": False,
}

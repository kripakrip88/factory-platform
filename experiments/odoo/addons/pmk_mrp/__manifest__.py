# -*- coding: utf-8 -*-
{
    "name": "ПМК — производство",
    "version": "19.0.1.0.0",
    "summary": "Список производственных заказов как в прототипе + честная загрузка рабочих центров",
    "description": """
Два экрана производства приведены к прототипу владельца.

СПИСОК ЗАКАЗОВ. Штатный список mrp.production показывает состояние документа
(«Черновик», «Подтверждено», «В процессе»), а цеху нужно другое слово:
просрочен заказ или нет. Просрочка — это НЕ состояние, а состояние плюс срок
плюс сегодняшняя дата, поэтому здесь заведено отдельное поле `pmk_status`
с четырьмя значениями прототипа: Просрочен / В работе / Не начато / Готово.
Плюс колонка «Клиент / источник», которой у производственного заказа нет
вообще: mrp.production не знает партнёра, клиент достаётся через строку заказа
на продажу. Если продажи за заказом нет, в колонку попадает штатный «Источник»,
и подпись колонки об этом честно предупреждает: у заказа из правила пополнения
там ссылка на документ, а не имя клиента.

Остальные колонки штатного списка (их набирается тринадцать) погашены в
optional="hide" — остаются в меню колонок, но список цеха открывается в пяти
колонках прототипа: № / Изделие / Клиент / Срок / Статус.

ЗАГРУЗКА РАБОЧИХ ЦЕНТРОВ. Полосы «участок — процент» со своим расчётом:
запланированные часы рабочих заданий за неделю, делённые на ёмкость календаря
рабочего центра за ту же неделю. Штатное поле kanban_dashboard_graph для этого
НЕ годится: при пустой таблице заданий оно подставляет случайные числа
(mrp/models/mrp_workcenter.py, randint) — см. подробный разбор в
models/mrp_workcenter.py. Когда считать не из чего, экран пишет «нет данных»,
а не ноль и не выдуманный процент.

ЧЕГО ЗДЕСЬ НЕТ НАМЕРЕННО. Маржи и денег на производственном заказе: модуль
sale_margin не установлен, а себестоимость (standard_price) у номенклатуры
пуста — любой показатель прибыли выглядел бы рабочим и был бы неверным.
""",
    "category": "Manufacturing",
    "author": "ПМК Парк",
    # Ровно два. mrp_workorder в зависимости брать НЕЛЬЗЯ: на стенде он
    # uninstallable (это модуль Enterprise), а сама МОДЕЛЬ mrp.workorder живёт
    # в обычном mrp — её нам и хватает.
    #
    # sale_mrp здесь тоже нет, хотя клиента мы достаём через его поле
    # sale_line_id. Причина в models/mrp_production.py: поле читается через
    # self._fields, а не жёсткой ссылкой, поэтому модуль работает и без него.
    "depends": ["mrp", "pmk_theme"],
    "data": [
        "views/mrp_production_views.xml",
        "views/mrp_workcenter_load_views.xml",
        "views/menus.xml",
    ],
    "license": "LGPL-3",
    "installable": True,
    "application": False,
}

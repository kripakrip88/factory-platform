# Factory Platform — Project Brain

## Что это

Цифровая платформа управления заводом металлоконструкций.
Монорепозиторий: github.com/kripakrip88/factory-platform

## Проблема которую решаем

- Заявки от клиентов приходят по email хаотично, обрабатываются вручную 2-5 дней
- КП считаются в Excel, долго согласовываются
- Производство планируется "в голове", нет видимости загрузки
- Данные разбросаны по таблицам, почте, мессенджерам

## Решение

Четыре сервиса в одном репо:

| Сервис | Что делает |
|--------|-----------|
| ERPNext | Единая база: клиенты, сделки, заказы, производство, склад, финансы |
| AI Assistant | Читает письма/PDF, извлекает параметры заявки, создаёт черновики КП |
| n8n | Связывает всё: email → ERP → уведомления → согласования |
| Telegram Bot | Уведомления и согласования прямо в Telegram |

## Сценарий работы

1. Клиент отправляет письмо с запросом
2. AI читает письмо, извлекает: тип изделия, параметры, сроки, контакты
3. В ERP автоматически создаётся лид + черновик КП
4. Менеджер получает уведомление в Telegram
5. Считается цена (материалы + работа + маржа)
6. Директор согласует КП кнопкой в Telegram
7. PDF автоматически уходит клиенту
8. После принятия — создаётся производственный заказ

## Архитектура AI (важно)

AI — не OCR-сервис. AI — центральный интеллект платформы.

```
services/ai-assistant/
├── api/              # HTTP endpoint для n8n
├── agents/
│   └── email/        # EmailAnalyzer — первый агент
├── prompts/          # промпты
├── parsers/          # структурированный вывод
├── integrations/
│   └── erpnext/      # REST API к ERPNext
├── tests/
└── knowledge/        # примеры писем, КП, тестовые данные
```

Роли AI на горизонте 12 месяцев:
- **Email Analyzer** — читает входящие, извлекает параметры заявки
- **Quotation Assistant** — считает КП из данных ERP
- **Production Assistant** — отвечает на вопросы о загрузке, просрочках
- **Drawing Analyzer** — извлекает размеры/материалы из PDF чертежей
- **ERP Copilot** — создаёт документы в ERPNext голосом/текстом

## Текущее состояние

| Компонент | Статус |
|-----------|--------|
| Структура репо | ✅ готова |
| GitHub репо | ✅ kripakrip88/factory-platform |
| Сервер | ✅ Бегет VPS, Ubuntu 24.04, IP: 155.212.143.179 |
| Docker + автодеплой | ✅ работает, GitHub Actions настроен |
| Docker Compose | ✅ проверен на сервере |
| ERPNext | ✅ запущен на порту 8080 |
| n8n | ✅ запущен, доступен через /n8n/ |
| PostgreSQL + Redis | ✅ работают |
| Nginx | ✅ health check + reverse proxy |
| GitHub Secrets | ✅ SERVER_HOST, SERVER_USER, SERVER_SSH_KEY |
| AI Assistant | ❌ пустая папка, нужно написать с нуля |
| Telegram Bot | ❌ только /start и /help, логика не реализована |
| n8n воркфлоу | ❌ пусто, нужно создать |

## Дорожная карта

**Месяц 1-2 — фундамент** ✅ в процессе
- ✅ Сервер Бегет VPS, Ubuntu 24.04
- ✅ Docker + автодеплой через GitHub Actions
- ✅ ERPNext v16 запущен (мигрировали с v15)
- ✅ Кастомные переводы ERPNext v16 (~14k строк, залиты в Translation DocType)
- ✅ Скрыты неиспользуемые модули (Бухучёт, Основные средства, Проекты, Качество, Субподряд)
- ⏳ Улучшенные переводы v2 — заводской глоссарий + правильные ERP-термины
- ⏳ n8n воркфлоу: email → лид
- ⏳ Email Analyzer → лид в ERP
- ⏳ Telegram-уведомление менеджеру
- ⏳ Автобэкап баз данных (PostgreSQL + brain-db) — cron job на сервере, дамп раз в сутки

**Месяц 3-4 — первая ценность**
- Quotation Assistant (считает КП из данных ERP)
- Согласование в Telegram
- PDF → клиенту автоматически

**Месяц 5-6 — расширение AI**
- Production Assistant
- Drawing Analyzer (PDF чертежи)
- Knowledge Base
- Qdrant — векторная база для Drawing/PDF Analyzer (семантический поиск по номенклатуре металла)

**Месяц 7-12 — AI Core полностью**
- ERP Copilot
- DWG/DXF анализ
- Роли-агенты с памятью (mem0)
- mem0 — персистентная память AI про клиентов и сделки

## Стек

| Слой | Технология |
|------|-----------|
| ERP | ERPNext v16 + встроенный CRM (Python, Frappe, MariaDB) |
| AI | NestJS, TypeScript, Claude API (claude-sonnet-4-20250514) |
| Оркестратор | n8n |
| Бот | Node.js, TypeScript, node-telegram-bot-api |
| БД | PostgreSQL (n8n, AI), MariaDB (ERPNext) |
| Кэш/очереди | Redis |
| Прокси | Nginx |
| Деплой | Docker Compose + GitHub Actions |
| Сервер | Бегет VPS, Ubuntu 24.04, IP: 155.212.143.179 |

## Деплой

- `develop` → staging
- `main` → production
- Деплой через SSH: `appleboy/ssh-action`
- Путь на сервере: `/opt/factory-platform`

## GitHub Secrets

| Secret | Описание |
|--------|----------|
| `SERVER_HOST` | IP сервера |
| `SERVER_USER` | SSH пользователь (deploy) |
| `SERVER_SSH_KEY` | Приватный SSH ключ |

## Правила для AI-ассистентов

1. Читай этот файл в начале каждой сессии
2. Перед задачей затрагивающей сервис — читай `services/<name>/README.md`
3. Показывай план и жди подтверждения перед изменениями
4. Не переусложняй — один рабочий агент лучше шести пустых папок
5. Все изменения пушить в `develop`, не в `main`
6. После каждой задачи давать ссылку на коммит по протоколу завершения
7. При любом изменении видимости полей, отключении модулей, секций или строк
   интерфейса ERPNext — обязательно обновлять `docs/disabled-features.md`.
   Документ содержит не только что отключено, но и что даёт каждый элемент
   при включении — сверяться с ним перед разработкой новой функциональности,
   чтобы не разрабатывать то, что уже есть в системе.
8. После каждого деплоя фронтенд-ассетов — проверять curl-ом что сервер отдаёт
   новую версию файла (маркер `SAAS_THEME_BUILD`), ПРЕЖДЕ чем тестировать
   функциональность в браузере. «Работает у меня после ручной копии» — не
   считается задеплоенным. Ручное копирование файлов в контейнеры ЗАПРЕЩЕНО.
   Подробности: `docs/deploy-assets.md`.

## Принцип изоляции модулей

Каждый новый модуль или эксперимент разрабатывается изолированно.

**Правила:**
1. Новый модуль — новая папка в `services/` или `experiments/`
2. Никаких изменений в `infra/docker-compose.yml` пока модуль не готов
3. Никаких изменений в `infra/nginx.conf` пока модуль не протестирован
4. Новый модуль не импортируется и не вызывается из существующих сервисов
5. Своя ветка: `feature/module-name` — не в `develop` пока не согласовано
6. Свой `docker-compose.yml` внутри папки модуля для локального запуска

**Папка для экспериментов:**

```
experiments/
  module-name/
    README.md          # что это, зачем, статус
    docker-compose.yml
    src/
```

**Перед тем как интегрировать модуль в основной стек** — явное согласование:
показать план интеграции, получить подтверждение.

## Протокол завершения задачи

После выполнения любого задания Claude Code обязан:

1. Запушить изменения в `develop`
2. Вывести в конце ответа блок:

```
✅ Готово
Коммит: https://github.com/kripakrip88/factory-platform/commit/<sha>
Изменено файлов: N
Следующий шаг: <одна фраза что делать дальше>
```

Это правило применяется ко всем задачам без исключения.
